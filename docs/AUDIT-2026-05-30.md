# Технический аудит AYANAKOJI-github — Wave 2026-05-30

**Дата:** 2026-05-30  
**Контекст:** продолжение [AUDIT-2026-05.md](./AUDIT-2026-05.md), scope single-user  
**Baseline:** `npm run build` OK · `npm test` 62/62 · `npm audit` 0 vulnerabilities

---

## Резюме

Закрыты оставшиеся риски из плана: race seed библиотеки, stale derived cache, prompt injection через `dailyLog.notes`, div0 в nutrition, cache stampede, fail-closed Groq proxy, async/error UX. Крупный рефакторинг (`context-builder`, unified readiness pipeline, multi-account IDB) **не выполнялся** — см. ограничения в плане.

| Метрика | До (05-29) | После (05-30) |
|---------|------------|---------------|
| Production readiness | 78 | **82** |
| Maintainability | 72 | **74** |
| Local-first cache correctness | 76 | **84** |

---

## Исправлено в этом цикле

### Критические — FIXED

| ID | Проблема | Файлы |
|----|----------|-------|
| C1 | Параллельный `ensureLibrarySeeded` → двойной seed | `library-books.ts` — single-flight + `bulkPut` |
| C2 | Groq proxy без `PROXY_TOKEN` | `workers/groq-proxy/src/index.js` — 503 без токена; `docs/DEPLOY.md` |

### Важные — FIXED

| ID | Проблема | Файлы |
|----|----------|-------|
| I1 | `useAsyncEffect` глотает ошибки | `useAsyncEffect.ts` — log + optional `onError` |
| I2 | Div0 nutrition | `nutrition-metrics.ts`, `NutritionDashboard.tsx` |
| I3 | `dailyLog.notes` в AI context | `context-builder.ts` — `truncateUntrustedText(500)` |
| I4 | Прямые `db.*` без revision bump | `revision-hooks.ts`, `App.tsx` — debounced hooks |
| I5 | Context cache stampede | `context-cache.ts` — in-flight dedupe |
| I6 | Silent export / palette DIRECTOR | `OsLayout.tsx` — `emitKernel` on error |
| I7 | Двойной `finish()` в ResonantBreath | `ResonantBreathLive.tsx` — `finishingRef` |
| I8 | OpsSummary без refresh | Уже было: Mind/Regulation/Influence |
| I9 | N+1 streak queries | `mind-metrics.ts`, `regulation-metrics.ts` — batch |
| I10 | Library sort после offset | `library-books.ts` — sort then slice |

### Рекомендации — частично

| ID | Действие | Статус |
|----|----------|--------|
| R1 | Удалить мёртвый metrics/compliance cache | **FIXED** |
| R2 | `clearDirectorPrompt` на factory reset | **FIXED** |
| R3 | CSP | Отложено (ломает Vite HMR в dev) |
| R4 | Firebase App Check | Отложено |
| R5 | Worker CORS restrict | Отложено |
| R8 | Multi-account IDB | Отложено (scope) |

---

## Новые тесты

- `src/core/engines/library-books.test.ts` — parallel seed, pagination order
- `src/core/cache/context-cache.test.ts` — concurrent dedupe
- `src/core/engines/nutrition-metrics.test.ts` — protein target 0

---

## Smoke checklist (ручной)

- [ ] Boot → OS после Firebase login
- [ ] COMMAND: toggle mission, compliance
- [ ] NUTRITION: % при нулевых targets
- [ ] ARCHIVE: export (palette), cloud sync
- [ ] DIRECTOR: palette briefing/debrief → ошибка в kernel log при сбое
- [ ] REGULATION: resonant breath complete (без двойного save)
- [ ] LIBRARY: первая загрузка без ошибки Dexie

---

## Отложено (вне scope)

- Полный рефакторинг `context-builder.ts` / `core/db/index.ts`
- Единый readiness pipeline
- Виртуализация всех списков
- Mass unused-export cleanup
- Multi-account IndexedDB isolation
- CSP meta / App Check / CORS origin lock

---

*Сгенерировано после Wave 2026-05-30. Предыдущие TOP-20: [AUDIT-2026-05.md](./AUDIT-2026-05.md).*
