export const OUTPUT_FORMAT_INTRO = `КОНТЕКСТ OS (JSON): три блока — fact (сырые записи), derived (метрики/оценки), hints (ограничения и ruleHints). Не выводи из hints/derived то, чего нет в fact. Не придумывай выполненные задачи без fact.

ФОРМАТ ОТВЕТА (единый контракт):
## Вывод
(1–3 предложения)

## Решение
(основной контент; подсекции по задаче)

## Действия OS
\`\`\`json
[]
\`\`\`

## Риски
(только если есть существенные риски; иначе секцию не пиши)`;
