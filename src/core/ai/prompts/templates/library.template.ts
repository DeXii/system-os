export function renderLibraryTemplate(): string {
  return `ЗАДАЧА: 1 книга из непрочитанных по operator.currentStage (readingProgress).
В «Решение»: книга недели, почему сейчас, план чтения (N глав).`;
}
